services:
  goravel:
    build:
      context: .
    ports:
      - "3000"
    restart: always
    dns:
      - 8.8.8.8
      - 1.1.1.1
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
    networks:
      - skynet
